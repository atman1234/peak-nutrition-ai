// Web-compatible mock for React Native PlatformColorValueTypes
// These are TypeScript types that don't exist on web but are referenced in RN StyleSheet

export const PlatformColor = () => null;
export const DynamicColorIOS = () => null;

// Type definitions for web compatibility
export default {
  PlatformColor,
  DynamicColorIOS,
};